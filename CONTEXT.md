# CONTEXT — life

> 每轮开工先读这里。详细需求见 `proposal.md` / `docs/PRD.md`，模块进度见 `docs/tasks/progress.md`。

## 项目一句话

帮自己把一天过得正常、清楚：今天唯一的一件事锚点 → 拆最小下一步 → 番茄钟 → 卡点转下一步 → 晚间复盘。
React 18 + TS + Vite 5，本地优先（localStorage `life-app-v1`），Supabase 快照同步，飞书日报，Capacitor 安卓壳。

## 当前架构（2026-08-11 UI 重构后）

```
src/
  App.tsx              应用外壳：侧栏 / 顶部 / 状态条 / Tab 切换 / 全局弹层（165 行）
  app/useAppRuntime.ts 所有跨页副作用：通知权限、原生提醒、应用锁、标题、空转提醒、飞书定时提交
  ui/                  设计系统：theme（强调色/明暗）、format、primitives（Card/Field/Toggle/Segmented/Modal…）
  views/               四个页面 + 悬浮计时器 + 过程笔记
  hooks/useLifeApp.ts  单一状态树 + actions（未改动，功能内核）
  lib/                 defaults / storage / sync / feishu / report / stats / review / mobileTimer / focusLock
  index.css            全站唯一样式表，token 驱动，浅色+深色
```

## 最近变更

- **2026-08-24 夜间：设计理念连贯性整改（一天一件第二轮）**
  用户点了三处「明显逻辑不对」，并要求把**其余理念不连贯的地方一起改**。
  从晨间锚点一路走到复盘，逐屏核对了一遍，共查出 20 条，本轮全部落地。

  **① 一个开始入口（用户点名）** —— 既然进页面就强制拆解、主屏显示的就是「下一秒手放在哪」，
  那"开始"的对象只能是当前那一步。删掉任务行级的「专注」按钮和步骤行里每步的「开始」，
  只留主行一个「开始」，它开的就是屏幕上那句话。连带修掉两处同源的不一致：
  主行原先显示 `steps[0]`（含已完成）而按钮开的是第一个**未完成**步骤（显示与动作指向不同的东西）；
  主行和步骤列表第一行是同一句话，重复显示。现在主行 = 当前这一步，列表只放它后面的。
  **没拆出步骤就没有「开始」，只给得出「＋ 拆一步」**——"填不出下一步就还不能开始"第一次真的成立。
  勾选框勾的也是当前这一步（无步骤时才是整件），**你勾的永远是屏幕上显示的那句话**。

  **② 「今天不做」从摆设改成唯一的换线动作（用户点名）** —— 原先是只读列表 + 「去任务池管理 →」，
  展开后一个动作都做不了，所以"显得没有意义"。现在每行只有一个动作「换上来」：
  把今天这一件换掉，原来那件掉回「今天不做」。不是同时做两件，是换。两步摩擦展开保留。

  **③ 「生活」退出今天页，到点提醒直接了结（用户点名）** —— 维护类没有"做不做"的决策余地，
  按《自律 App 设计要点》第 3 条本就不该进任何清单。今天页删掉「生活」折叠区；
  到点的提醒横幅**带上「做完了」按钮**（`runtime.reminder` 从字符串改成 `{ message, itemId }`），
  就地打勾。连带三处：所有 routine 每天都补今日副本（原先 `.slice(0, 2)` 截断，
  第 3 条之后提醒了也没地方打勾）；routine 的今日副本**不再塞假步骤**「完成：xxx」
  （它会混进 DayBar 刻痕和飞书「做完的步骤」）；`stats.ts` 把 routine 踢出完成率与热力图分母。

  **④ 删掉「边界」（用户点名）** —— 它和任务池「长期规则」的 ✗ 红线是同一件事的两个入口；
  "今天不碰什么"打勾语义本身就不成立（不碰是持续状态，勾完还能再犯）；`N/M` 又是一个完成率。
  红线交给长期规则。连带删 `AvoidItem` / `avoidItems` / `addAvoidItem` / `toggleAvoidDone`。

  **⑤ 删掉「放松窗口」整套** —— 和"番茄结束自动进入 5 分钟休息"是两套并行的休息机制；
  推荐语「去看 30 分钟影视解说放松一下」与设置里的专注阻断名单（抖音 / B 站）自相矛盾。
  删 `RelaxWindow` 类型、`relaxWindows` 字段、横幅、`consumeRelaxWindow`、设置页「放松窗口（分）」。

  **⑥ 删掉「今天和人认真聊过」** —— 这正是用户说的"给一件生活类的事再单加一行"，
  而且与 routine「主动联系一个人 20:30」重复。留后者的到点提醒，删前者的勾选框、
  `setCommunication`、`communicationDone/Note`、飞书报文的「交流：」行。

  **⑦ 任务池不再是第二个开始入口** —— 「进今天」（把事塞进今天的待办）和「直接开始」
  （不经过拆步骤直接开专注）都与"一天一件 + 单一开始入口"冲突。两个按钮合成一个
  「换成今天这一件」，跳回今天页后在那一步上按「开始」。`addTaskToToday` /
  `launchTaskDefinition` 两个 action 随之删除；生活提醒行的「进今天」也删（它每天自动进）。

  **⑧ 收纳箱做完前不可见** —— 文档第 4 条要求"完成当前任务后才弹出收纳箱供挑选"，
  但「好奇清单」原先常驻页脚、带计数、随时可展开。改成：主线没做完时按钮是「记一笔」，
  弹层只有输入框、写完即关、不列内容不显示计数；这一件做完了才变回「好奇清单 N」并摊开。
  主线做完后区块也不再是空白，会明说"今天这一件做完了"。

  **⑨ 三个创建入口的 `nextStep` 口径统一** —— 任务池「加一件」把它写进 `taskDef.nextStep`，
  但晨间锚点和今天页「快速加一件」只把它当今天的第一步、**不写进任务定义**，
  以后再从任务池启动就会丢掉这句、退回「先开始：xxx」的兜底文案。现在三处一致。

  **⑩ 清掉一批遗留** ——
  晨间锚点不再按 `topTaskSlots` 循环渲染带序号（三件事时代的遗留），改成单件表单；
  主线区块不再显示「今天已完成 N 件」、完成框不再显示永远是 1 的序号（文档：不显示计数/完成率）；
  删掉 ↑↓ 排序按钮（主线只显示一件，排序无作用，且排序=决策）；
  `dailyTemplate` 整个对象删除（`topTaskSlots` 已被 storage 强制拉回 1，
  `routineSlots`/`avoidSlots`/`communicationPrompt`/`relaxMinutes` 全是死字段）；
  `createTodayItemFromTask` 在 `defaults.ts` 和 `useLifeApp.ts` 各有一份完全相同的实现，合并；
  `defaultTaskDefs` 里的主动任务没有 `nextStep`，违反自己定的强制规则，补上；
  复盘页对"今天这一件"的口径（`slice(0, topTaskSlots)`）与今天页（第一个**未完成**的 normal）
  不同，统一成"所有主动任务"；`review.tomorrow` 是死字段（表单从不渲染它，
  飞书报文却每天输出「明天第一步：还没写。」），报文行删掉、字段标为遗留只用于回看老记录。

  **⑪ `loadData` 改成逐字段挑选** —— 原先 `{...fallback, ...parsed}` 会让删掉的功能
  以死键形式永远留在快照里，还会被同步推到云端。现在旧快照里的 `avoidItems` /
  `communicationDone` / `dailyTemplate` / `relaxWindows` 读一次就真的没了。

  **验证**：浏览器扩展连不上 localhost（三次同一报错，没在上面耗），改用真实渲染走查，
  并把它固化成常驻护栏 `src/__tests__/today.flow.test.tsx`（6 条）：锚点两格才放行、
  页面上只有一个「开始」且开的是当前这一步、没步骤就没有开始、生活类不在今天页占行、
  第二件事只能进「今天不做」且只能换、收纳箱做完前不可见。
  四项门槛全绿：test 47 / typecheck / lint / build。

  **本轮做的假设**（夜间模式无人值守，按最稳妥假设推进，早上请裁决）：
  - 用户说「今天不做」没意义，但它是《设计要点》最小可用版本四件事之一，所以**没删，改成有用的**
    （唯一动作「换上来」）。若确实想删，一句话的事。
  - 「今天和人认真聊过」判定为与 routine「主动联系一个人」重复而删除。飞书日报少一行「交流」。
  - 「卡住了」仍是两层菜单（2026-08-17 用户已拍板保留），连续打卡 streak /
    统计面板 / 热力图同样保留不动。

- **2026-08-20 修掉 Cloudflare Pages 构建失败，顺手更新安卓安装包**
  Cloudflare 部署连续报了两轮错，根因都在 `package-lock.json`：
  1. 旧锁文件里塞了大量陈旧冗余依赖（760 包，本该只有 ~545），`vitest@4` 嵌套的
     `vite@8` 需要 `esbuild ^0.27/^0.28`，却被去重到过时的 `esbuild@0.21.5`，
     本地 `npm ls` 能看出这条 `invalid`；Cloudflare 的 `npm ci` 严格校验，直接报
     `Missing: esbuild@0.28.2 from lock file` 退出。
  2. 用本机 npm 11.12.1 重新生成锁文件后，esbuild@0.28.2 的 26 个平台子包被标成
     `extraneous` 而不是 `optional`；Cloudflare 用的是 npm 10.9.2，`npm ci` 把
     `extraneous` 当必装，于是又报 `EBADPLATFORM`（想装 netbsd-arm64 到 linux 机器）。
  **教训：本机 npm 版本要和部署环境（Cloudflare 用 npm@10.9.2）对齐，否则同一份
  依赖树在锁文件里的 `optional`/`extraneous` 标记会不一致。** 最终用
  `npx npm@10.9.2 install` 重新生成锁文件，`npm ci` + `npm run build` 本地验证通过。
  顺手用最新代码跑了一遍 `npm run android:apk:debug`，把新调试包同步进
  `public/downloads/life-android.apk`（网页下载入口用的就是这个文件，随 Cloudflare
  这次部署一起更新）。
  本机 Android 构建环境的两个隐坑记一下：Windows 下 npm 默认用 cmd.exe 执行
  `npm run` 脚本，脚本里的 `./scripts/xxx.sh` / `rm -rf` 在 cmd 里跑不了，要么设
  `npm_config_script_shell=/usr/bin/bash`，要么直接手动 `export` 好环境变量后调
  `bash scripts/android-gradlew.sh`；PATH 里的 `python3` 是 Windows Store 的空壳
  （静默失败，exit 49），`scripts/android-repo-proxy.py`（绕 Google/Maven 仓库
  DNS/TLS 问题用的本地代理）必须用真实解释器（本机是 `/d/python/python3.13/python`）
  手动起，否则 Gradle 连不上 `127.0.0.1:4873` 直接报 `Connection refused`。

- **2026-08-18 修掉「小黑棍」：设置页 SQL 折叠区的浏览器默认 marker**
  用户看到展开箭头旁边多一根"斜着的小黑棍"。查下来是 `details.mini`（设置 → 跨设备同步 →
  「Supabase 建表 SQL」）**漏了压制原生 marker**——全站其它 `<details>`（`.fold`、`.history`）
  都写了 `list-style: none` + `::-webkit-details-marker { display: none }`，只有它没写，
  于是 Chrome 画了自己那个实心三角，比自定义的 ∨/∧ 描边箭头黑得多。补上两行即可。
  顺手删掉 `.principles` 样式——上一轮删「核心原则」区块时漏下的死 CSS。

- **2026-08-17 一天一件改造：落地《自律 App 设计要点》的拍板结果**
  本轮通过两次 AskUserQuestion 把《自律 App 设计要点》与现有实现的 9 条冲突和结算问题逐条问清楚，
  结论：**保留现状**（不按文档改）的是——连续打卡 streak、统计面板/热力图、任务计数、
  定时提醒、维护任务进清单打勾、收纳箱随时可见、复盘开放日记+评分、卡住了两层菜单；
  **新增**——创建任务强制填「下一秒手放在哪」且主屏显示该字段而非标题、「今天不做」折叠区加两步摩擦；
  **精简设置页**——删掉「本周模板」「核心原则」两个折叠区（前者是文档反对的自我规划配置，
  后者内容已被「一天一件」证伪且与新增字段/答案手册重复），同步/飞书/专注阻断保留（真功能，非装饰参数）。
  实现要点：
  - `dailyTemplate.topTaskSlots` 默认 `3→1`；`lib/storage.ts` 对旧快照也强制拉回 1（这条不再可配置，设置页「今日几件事」输入框已删）。
  - 晨间锚点、复盘"明日"表单都按 `topTaskSlots` 循环，槽位数变了不用改结构，只改文案为单数。
  - 复用现有 `TaskStep`/"预拆第一步"机制做强制字段：`TaskDefinition.nextStep`，任务池"加一件"/
    今天页快速加一件/晨间锚点三个创建入口都要求非空；`TaskRow` 主展示行改成第一个步骤的标题，任务标题降为副行。
  - `ui/primitives.tsx` `Fold` 新增 `friction` prop：两阶段展开（先出"确定要看"确认行，点它才真正展开）。
  - 今天页"主线"只留一条进行中的项，其余待做的（deadline 自动同步/之前多加的）+ 任务池未启动的
    normal 任务，收进新增的「今天不做」摩擦折叠区（只读，管理仍在任务池页）。
  - 删除 `lib/principles.ts`、`WeeklyTemplate` 类型/`updateWeeklyTemplate` action。
  - 浏览器实机走过一遍：晨间锚点单槽位确认 → 主线单件 + 下一步展示 → 完成后自动腾位 →
    「今天不做」两次点击展开 → 任务池"加一件"必填下一步 → 设置页确认两个折叠区消失、同步/飞书仍在。
  - 四项门槛全绿：test 40（principles.test.ts 随删）/ typecheck / lint / build。

- **2026-08-17 第五轮补刀：一屏东西太多 → 说明文字不再常驻**
  （用户反馈：一个界面展示太多东西，看着烦；把没用的删掉）
  - **定的规矩：说明只在你看不见内容的时候出现。** 常驻的解说句是给第一次用的人看的，
    天天看就是噪音。所以日常页（今天 / 任务池 / 复盘）的区块说明全部删掉，
    引导词并进**空状态**和**输入框提示**；折叠区块的说明只在收起时显示，展开就让位。
    只有「设置」保留常驻说明——那是查阅型页面，本来就靠说明找东西。
  - 删掉的东西（都能补回来）：
    - **顶部鼓励文案**（连同 `encouragementMessages`、`settings.encouragementEnabled`、设置里的开关）——纯装饰。
    - **「从任务池拉进来：xxx」建议行**——和页边栏的「任务池 →」说的是同一件事。
    - **页脚「去复盘 →」**——顶栏和底栏都有这个 tab。
    - **任务行的「N/M 步」**——步骤就列在下面，数字是重复的；只留「截止 x」。
    - **每页一句的 `PAGE_LEDE`**——四个页面的开场白。
    - 失效样式 `.quiet-row`（随建议行一起没用了）、`.pill`（上一轮换成批改记号列后没人引用）。
  - 已经拆过步骤的任务，「＋ 拆一步」退到 hover 才出现——同一句话不在页面上重复三遍。
  - 今天页从「页头 + 4 区块 + 说明句若干 + 页脚四项」收成「页头 + 3 区块 + 页脚三项」，一屏半看完。
  - 四项门槛全绿：test 42 / typecheck / lint / build。

- **2026-08-17 第五轮：把「方格纸」这件事做到底——手写体 + 落格 + 红页边线**
  （用户反馈：既然是方格纸，字也该是手写的；字要写进格子里；要有分隔线；按钮和整体不搭；「过程笔记」格式要改）
  - **三套字，按「谁写的」分工**，这是这一版全部的规矩：
    `--hand` 手写（拉丁 Caveat + 中文落系统楷体）＝ 标题 + **你亲手写下的内容**（任务、步骤、规则、边界、笔记、复盘）；
    `--mono` IBM Plex Mono ＝ **机器给的**时间、计数、序号、密钥；`--sans` 系统无衬线 ＝ 按钮和说明文字。
    中文手写靠系统楷体（Win 的 KaiTi / mac 的 Kaiti SC），不下载中文字体；Caveat 只拉丁子集，离线可用。
  - **落格**：`--cell: 24px` 成为唯一纵向单位。根 line-height = 24px，顶栏 47+1、页面上留白 48、区块间距 48 全是它的倍数。
    真正的横格纸铺在三处「你在这里写字」的界面：过程笔记编辑器、复盘的多行输入、拆步骤列表（后者用逐行下划线，
    因为行里有按钮、行高会超过一格，背景格线会漂）。
  - **红页边线**：`.page::before` 一条竖线，落在页边栏与正文栏之间，跑满整页——作业本的那条线。窄屏隐藏。
  - **按钮全部改成方的**：`--edge: 3px` 是全站唯一圆角。分段控件从灰底药丸改成并排方格（选中＝红笔涂实）；
    **开关从 iOS 滑块改成和任务框一样的红铅笔勾**——全站只有一种「打上了」的记号。
  - **格式修复**：
    - 行列表改三栏网格（标题 / 元信息 / 操作），时间与截止日期永远对齐成一竖列。
    - 长期规则从「pill + 文字」改成左侧一列批改记号：红 ✓ ＝ 要守的方向，灰 ✗ ＝ 红线；加规则表单一行摆平不再换行。
    - 任务池「加一件」的日期/时间输入不再拉满整行；表单里的按钮不再长成一条横幅。
    - 过程笔记：日期做页头（手写）+ 红线分隔，正文是横格纸，时间戳改成页边一枚红色钟点而不是 `--- 09:20 ---`。
    - `DayBar` 从「白底方框」改成画在纸上的一把尺：一条基线 + 红杠（专注）+ 刻痕（勾掉的步）+ 竖线（现在）；修掉「收工」标签被挤成竖排。
  - **修掉一个真 bug（影响之前所有折叠区块）**：Chrome 131+ 给 `<details>` 加了 `::details-content` 匿名包裹层，
    正文不再是 `<details>` 的直接网格项，`grid-column: 2` 失效 → 所有 Fold（生活/长期规则/本周模板/阻断/同步/飞书/原则）
    的正文都掉进 112px 的页边栏那一列，被压成一列单字。改成 `<details>` 不当网格容器，正文用左边距对齐正文栏。
  - 浏览器实机逐页核对过（今天/任务池/复盘/设置/过程笔记/深色），四项门槛全绿：test 42 / typecheck / lint / build。

- **2026-08-12 第四轮：定下视觉身份「方格纸与红铅笔」**
  - 方向来自主体本身：这是一个人的日课本。淡方格线打底（24px 一格），
    强调色换成红铅笔 `#c4342b`，只用在「你亲手做的记号」上——勾选、当前项、主按钮、时间轴上的痕迹。
  - 自托管两款字体（`@fontsource`，无 CDN、离线可用）：Archivo 承担拉丁标题，
    IBM Plex Mono 承担所有数字与时间。中文走系统字体，所以让数字和拉丁当门面，正好贴合「仪表」这个方向。
  - **签名元素 `DayBar`（`views/DayBar.tsx`）**：页标题下一条真实时间轴，07:00 → 收工时间。
    红杠 = 完成的一轮专注，刻痕 = 勾掉的一步，竖线 = 现在，收工之后是斜线区。
    数据全部来自已有的 focusSessions / steps.completedAt / settings.hardStopTime。
  - 删掉上一轮的 `DayRule` 刻度尺（●●○）——那是装饰，不是信息。

- **2026-08-12 第三轮：确定视觉方向「日志本」**（用户反馈：字号主次不分、分不清谁和谁一组、该藏的没藏、缺标题和空间）
  - 结构即信息：每个区块 = 左侧 112px 页边栏（标题 + 计数 + 跳转链接）+ 右侧正文栏。
    分组靠位置区分，正文里一个描边都不留。页边栏 sticky，长区块滚动时始终知道自己在哪一组。
  - 字号阶梯拉开：页标题 30 / 页边栏标题 15（实心黑）/ 行标题 15.5 / 正文 15 / 说明 13.5 / 数字 11.5 等宽。
  - 所有数字（时间、计数、日期、序号、统计）统一用等宽字体，成为页面的固定纹理。
  - 签名元素 `DayRule`：页标题下的刻度尺，一件主线一格，完成填实，后面缀完成的番茄轮数。
  - 每个区块补一句 `desc` 说明它是干嘛的；次要区块（生活、本周模板、阻断、同步、飞书、原则）走 `Fold` 默认收起。
  - 任务行的已完成步骤默认折起，只留「已完成 N 步」；跳转入口：主线→任务池、今天→复盘。
  - 底色从暖奶油改成冷灰蓝纸面，默认强调色墨蓝；避免「奶油+衬线+赤陶」这套 AI 默认皮。

- **2026-08-12 视觉重做（第二轮）**：上一版仍是「一堆等重的卡片」，这轮换成版式驱动。
  - 左侧栏删掉，改顶部细导航（品牌 / 4 个 tab 下划线 / 计时器 / 笔记）；移动端底栏。
  - 内容收成单栏 660px，区块之间靠留白 + 发丝线分隔，取消卡片描边和阴影。
  - 计时器从「顶部横幅 + 悬浮窗 + 状态条」三处合并成顶栏一颗药丸 + 点开的浮层。
  - 今天页从 9 个区块砍到 3 个（主线 / 不做 / 生活）+ 一行页脚入口（卡住了、好奇清单、交流）。
  - 删除重复：现在做这个卡、执行守则常驻卡、交流卡、侧栏统计、悬浮笔记按钮。
  - 复盘页并成单栏：统计 → 复盘表单 → 今天的轨迹 → 历史（折叠）。
  - 按钮统一成三种：primary 实心 / btn 描边 / link 纯文字，外加 icon-btn。
  - 删代码：`src/components/TimePicker.tsx`、`feishu.ts` 的 `sendTodayReportToFeishu` 及其私有
    `buildFeishuBody` / `buildParagraphs` / `createTextLine`、`format.ts` 的 `greeting` / `formatClock`、
    `helpers.ts` 的 `stepProgressOf`。


- **2026-08-11 UI 全量重构**：App.tsx 3341 行 → 165 行；index.css 2693 行 → token 化设计系统。
  - 四层横幅（topbar / focus-strip / feedback-strip / context-reminder）合并成一条 `.status` 状态条。
  - 晨间锚点未确认时整页只剩锚点表单（D1 硬阻断的界面化）。
  - 新增浅色/深色外观（`settings.appearance`，默认跟随系统），5 套强调色保留。
  - 移动端改为底部 Tab 栏，不再靠 `isMobileLayout` 在 JSX 里分叉。
  - 飞书报文抽到 `lib/report.ts`，今天/历史/定时/复盘四处共用；过程笔记发送前转纯文本。
  - 新增 `src/__tests__/app.render.test.tsx` 渲染护栏；`vitest.setup.ts` 补 matchMedia。
  - 四项门槛全绿：test 42 / typecheck / lint / build。

## 当前任务

**等用户裁决 2026-08-24 夜间这一轮**（见上面"最近变更"的三条假设）。裁决之后：

1. 若认可，`git add -A && git commit`（提交信息草稿在 `docs/tasks/progress.md` 的「待提交 Git」）。
2. 窄屏（≤760px）实机核对——本轮改了今天页结构、`.gate-fields`（锚点两格）和「今天不做」
   行内新增的「换上来」按钮，都只在桌面宽屏的渲染测试里验证过。
   验收标准＝窄屏下这三处不折行错位、按钮可点；建议档位：低。

## 待确认问题（夜间模式留给用户裁决）

- **「今天不做」的去留**：用户说它"没有什么意义"，但《设计要点》第 1 条把它列进最小可用版本。
  本轮的处理是保留区块、给它唯一一个真动作「换上来」。若还是觉得多余，删掉即可。
- **「今天和人认真聊过」删得对不对**：判断依据是它与 routine「主动联系一个人 20:30」重复，
  且正是用户说的"再单加一行"。代价是飞书日报少一行「交流：已完成/未完成」。
- **`tomorrowTop3` / `tomorrowTop3Steps` 字段名**是三件事时代的遗留，现在界面上只写第 0 格。
  改名要连带迁移已存的复盘数据，本轮没动。

## 待办 / 已知遗留

- 今天页本轮结构大改（主行=当前这一步、「今天不做」行内多了「换上来」按钮），
  加上 `.gate-fields`（晨间锚点两格输入）和 `.add`（快速加一件三件套），
  **窄屏（≤760px）都没有实机核对**。
- `lib/review.ts` 的 `getAnchorPrefillFromReview` / `getNextDayAnchorPrefill` / `isValidMoodScore`
  只有测试在用（候选清理，等确认——删它们要连带删测试）。
- Caveat 的西里尔子集会打进 dist（约 120KB）：受 unicode-range 限制永远不会被下载，
  Service Worker 也只预缓存应用外壳，不影响体积感知。@fontsource 没提供纯拉丁入口，暂不处理。
- 窄屏（≤760px）本轮只改了 CSS，没有实机核对过。
